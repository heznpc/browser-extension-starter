{
  "name": "browser-extension-starter",
  "private": true,
  "scripts": {
    "build:chrome": "rm -f dist/chrome.zip && mkdir -p dist && zip -r dist/chrome.zip manifest.json src/ assets/icons/ -x '*.DS_Store'",
    "build:firefox": "rm -f dist/firefox.zip && mkdir -p dist && zip -r dist/firefox.zip manifest.json src/ assets/icons/ -x '*.DS_Store'",
    "test": "jest --verbose --passWithNoTests",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
